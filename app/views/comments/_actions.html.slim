-if user_signed_in? && resource.user == current_user
  =link_to 'Редактировать', [:edit, parent, resource], remote: true
  | |
  =link_to 'Удалить', [parent, resource], data: { method: :delete, remote: true, type: :json, confirm: 'Вы уверены, что хотите удалить комментарий?' }