-if user_signed_in? && @question.user != current_user && !@question.voted_by?(current_user)
  =link_to 'Нравится', like_question_path(@question), remote: true
  '|
  =link_to 'Не нравится', dislike_question_path(@question), remote: true
