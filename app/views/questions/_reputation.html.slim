-if user_signed_in? && resource.user != current_user && !resource.voted_by?(current_user)
  =link_to 'Нравится', like_question_path(resource), remote: true
  '|
  =link_to 'Не нравится', dislike_question_path(resource), remote: true
