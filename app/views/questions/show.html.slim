.js-question
  .topic=@question.title
  .body=@question.body
  p Вложенные файлы:
  ul
    = render @question.attachments
  .actions
    =render 'actions'
  .answer-form
  .comment-form
  .comments
    =render @question.comments
.js-answers
  =render @question.answers
