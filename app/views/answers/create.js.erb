<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |message| %>
    $('.js-answer-errors').before('<%= j message %>')
  <% end %>
<% else %>
  $('.answers').html('<%= j render 'list' %>');
  $('#answer_body').val('');
<% end %>