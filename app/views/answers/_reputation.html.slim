-if user_signed_in? && resource.user != current_user && !resource.voted_by?(current_user)
  =link_to 'Нравится', voteup_answer_path(resource), remote: true
  '|
  =link_to 'Не нравится', votedown_answer_path(resource), remote: true